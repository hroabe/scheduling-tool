# RFC-0006: 多言語対応（i18n）

## 概要
日程調整ツールを多言語対応し、日本国内の外国人ユーザーや国際チームでの利用を可能にする。

## 背景
- 日本国内の在日外国人（約300万人）、外資系企業、国際チームからのニーズ
- 日本語ネイティブ品質 + 多言語対応の組み合わせは競合の空白地帯
- 実装コストが低く、差別化効果が高い

## 対象言語（優先順）
1. **Phase 1**: 日本語（既存）+ 英語
2. **Phase 2**: 中国語（簡体/繁体）
3. **Phase 3**: 韓国語、ベトナム語、ポルトガル語

## 設計方針

### Backend (Django)
- Django標準の `django.utils.translation` を使用
- Accept-Language ヘッダーで言語自動検出
- ユーザー設定で言語固定可能（UserProfile.preferred_language）
- メッセージカタログ: `api/locale/{lang}/LC_MESSAGES/django.po`

### Frontend (Next.js)
- `next-intl` または `next-i18next` を使用
- URL構造: `/{lang}/...` （例: `/en/schedule/...`）
- 言語切替UI（フッターまたはヘッダー）
- ブラウザ言語自動検出 + Cookie保存

### 翻訳対象
1. **UI文字列**: ボタン、ラベル、エラーメッセージ
2. **メール通知**: 件名、本文テンプレート
3. **API エラーメッセージ**: バリデーションエラー等
4. **日付/時刻フォーマット**: ロケール対応

## API変更

### ヘッダー
```
Accept-Language: ja, en;q=0.8
```

### UserProfile拡張
```python
class UserProfile(models.Model):
    preferred_language = models.CharField(
        max_length=10,
        default='ja',
        choices=[('ja', '日本語'), ('en', 'English'), ...]
    )
```

### Schedule拡張（オプション）
```python
class Schedule(models.Model):
    # 公開ページのデフォルト言語
    default_language = models.CharField(max_length=10, default='ja')
```

## フロントエンド構造
```
front/
├── messages/
│   ├── ja.json
│   ├── en.json
│   └── zh.json
├── app/
│   └── [lang]/
│       ├── page.tsx
│       └── schedule/
└── middleware.ts  # 言語ルーティング
```

## 翻訳ファイル例

### ja.json
```json
{
  "common": {
    "submit": "送信",
    "cancel": "キャンセル",
    "save": "保存"
  },
  "schedule": {
    "title": "日程調整",
    "createNew": "新規作成",
    "finalize": "確定する"
  }
}
```

### en.json
```json
{
  "common": {
    "submit": "Submit",
    "cancel": "Cancel",
    "save": "Save"
  },
  "schedule": {
    "title": "Schedule",
    "createNew": "Create New",
    "finalize": "Finalize"
  }
}
```

## 実装フェーズ

### Phase 1: 基盤構築 + 英語対応
1. Backend: Django i18n設定、ミドルウェア
2. Frontend: next-intl導入、ルーティング
3. 翻訳ファイル作成（ja/en）
4. 言語切替UI

### Phase 2: アジア言語対応
1. 中国語（簡体/繁体）翻訳
2. 日付フォーマットのロケール対応
3. タイムゾーン表示の多言語化

### Phase 3: 追加言語
1. 韓国語、ベトナム語、ポルトガル語
2. 翻訳管理ワークフロー確立

## 非機能要件
- 翻訳キー追加時にビルドエラー（TypeScript型チェック）
- 未翻訳キーはデフォルト言語にフォールバック
- 翻訳ファイルはJSON形式で管理しやすく

## テスト計画
- 各言語でのUI表示確認（スクリーンショット比較）
- 日付/時刻フォーマットの正確性
- メール通知の言語切替

## 関連RFC
- RFC-0003: UserProfile に preferred_language を追加
- RFC-0005: 1対1予約ページの多言語対応
